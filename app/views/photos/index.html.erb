<ul class="cbp-rfgrid">	
	<% @photos.each do |photo| %>
		<li><a href="<%= photo.url %>" target='_blank' alt="<%= photo.description %>">
			<img src="<%= photo.photo_url %>"></li></a>
	<% end %>
</ul>